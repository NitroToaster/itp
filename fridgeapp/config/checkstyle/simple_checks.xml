<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
          "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
          "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
    Simple Checkstyle Configuration
    Focuses on basic code formatting and whitespace only
-->

<module name="Checker">

  <property name="charset" value="UTF-8"/>
  <property name="severity" value="warning"/>
  <property name="fileExtensions" value="java"/>

  <!-- Exclude module-info.java files -->
  <module name="BeforeExecutionExclusionFileFilter">
    <property name="fileNamePattern" value="module\-info\.java$"/>
  </module>

  <!-- Whitespace Checks -->
  <module name="FileTabCharacter">
    <property name="eachLine" value="true"/>
  </module>

  <module name="TreeWalker">

    <!-- Whitespace After -->
    <module name="WhitespaceAfter">
      <property name="tokens" value="COMMA, SEMI, TYPECAST"/>
    </module>

    <!-- Whitespace Around -->
    <module name="WhitespaceAround">
      <property name="allowEmptyConstructors" value="true"/>
      <property name="allowEmptyMethods" value="true"/>
      <property name="allowEmptyTypes" value="true"/>
      <property name="allowEmptyLoops" value="true"/>
      <property name="tokens" value="ASSIGN, BAND, BAND_ASSIGN, BOR, BOR_ASSIGN, BXOR,
                    BXOR_ASSIGN, DIV, DIV_ASSIGN, EQUAL, GE, GT, LAND, LE, LOR, LT, MINUS,
                    MINUS_ASSIGN, MOD, MOD_ASSIGN, NOT_EQUAL, PLUS, PLUS_ASSIGN, QUESTION,
                    SL, SL_ASSIGN, SR, SR_ASSIGN, STAR, STAR_ASSIGN"/>
    </module>

    <!-- No Whitespace Before -->
    <module name="NoWhitespaceBefore">
      <property name="tokens" value="COMMA, SEMI, POST_INC, POST_DEC, DOT"/>
      <property name="allowLineBreaks" value="true"/>
    </module>

    <!-- Parenthesis Padding -->
    <module name="ParenPad">
      <property name="tokens" value="CTOR_CALL, CTOR_DEF, LITERAL_CATCH, LITERAL_DO,
                    LITERAL_FOR, LITERAL_IF, LITERAL_NEW, LITERAL_SWITCH,
                    LITERAL_SYNCHRONIZED, LITERAL_WHILE, METHOD_CALL, METHOD_DEF"/>
    </module>

    <!-- Empty Line Separator -->
    <module name="EmptyLineSeparator">
      <property name="tokens" value="IMPORT, STATIC_IMPORT, CLASS_DEF, INTERFACE_DEF,
                    ENUM_DEF, STATIC_INIT, INSTANCE_INIT, METHOD_DEF, CTOR_DEF"/>
      <property name="allowNoEmptyLineBetweenFields" value="true"/>
      <property name="allowMultipleEmptyLines" value="false"/>
    </module>

    <!-- One Statement Per Line -->
    <module name="OneStatementPerLine"/>

    <!-- Array Type Style -->
    <module name="ArrayTypeStyle"/>

    <!-- Upper Ell (use 'L' not 'l' for long literals) -->
    <module name="UpperEll"/>

    <!-- Left Curly Brace -->
    <module name="LeftCurly">
      <property name="option" value="eol"/>
    </module>

    <!-- Right Curly Brace -->
    <module name="RightCurly">
      <property name="option" value="same"/>
      <property name="tokens" value="LITERAL_TRY, LITERAL_CATCH, LITERAL_FINALLY,
                    LITERAL_IF, LITERAL_ELSE, LITERAL_DO"/>
    </module>

    <!-- Need Braces -->
    <module name="NeedBraces">
      <property name="tokens" value="LITERAL_DO, LITERAL_ELSE, LITERAL_FOR,
                    LITERAL_IF, LITERAL_WHILE"/>
    </module>

    <!-- Avoid Star Import -->
    <module name="AvoidStarImport"/>

    <!-- No Line Wrap -->
    <module name="NoLineWrap">
      <property name="tokens" value="PACKAGE_DEF, IMPORT, STATIC_IMPORT"/>
    </module>

    <!-- Generic Whitespace -->
    <module name="GenericWhitespace"/>

    <!-- Method Parameter Padding -->
    <module name="MethodParamPad"/>

    <!-- Operator Wrap -->
    <module name="OperatorWrap">
      <property name="option" value="nl"/>
      <property name="tokens" value="BAND, BOR, BSR, BXOR, DIV, EQUAL, GE, GT, LAND, LE,
                    LITERAL_INSTANCEOF, LOR, LT, MINUS, MOD, NOT_EQUAL, PLUS, QUESTION,
                    SL, SR, STAR"/>
    </module>

    <!-- Empty Catch Block -->
    <module name="EmptyCatchBlock">
      <property name="commentFormat" value=".*"/>
    </module>

    <!-- Multiple Variable Declarations -->
    <module name="MultipleVariableDeclarations"/>

  </module>
</module>